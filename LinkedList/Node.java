class Node {
  /* 2 attributes: data for int value in node,
  * next to point to next node */
  int data;
  Node next;

  // constructor for creating object nodes and assigning values to them
  Node(int data) {
    this.data = data;
  }
  
}
